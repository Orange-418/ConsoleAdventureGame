#ifndef INPUTHANDLER_H
#define INPUTHANDLER_H
#include <vector>

class inputHandler
{
    public:

    int returnX(std::vector<std::vector<char>> map, int sizeX, int sizeY);
    int returnY(std::vector<std::vector<char>> map, int sizeX, int sizeY);

    private:
};

#endif

